services:
  - type: web
    name: signspeak-ai
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --chdir backend --timeout 120 app:app
    autoDeploy: true
